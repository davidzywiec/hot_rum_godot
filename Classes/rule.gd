class_name Rule

var round_number: int
var description: String
var round_name: String
var num_of_cards: int

func _init(rnd_num, desc, rnd_name, num_cards):
    round_number = rnd_num
    description = desc
    round_name = rnd_name
    num_of_cards = num_cards
    